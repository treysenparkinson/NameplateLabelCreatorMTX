[build]
publish   = "."
functions = "netlify/functions"
command   = "npm ci"

[functions]
# Bundle node_modules used by functions (pdf-lib, AWS SDK, etc.)
node_bundler = "esbuild"

[build.environment]
NODE_VERSION = "18"

